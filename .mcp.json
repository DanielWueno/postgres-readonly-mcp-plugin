{
  "mcpServers": {
    "postgres-readonly": {
      "command": "${CLAUDE_PLUGIN_DATA}\\venv\\Scripts\\postgres-mcp.exe",
      "args": ["--access-mode=restricted"],
      "env": {
        "DATABASE_URI": "${user_config.database_uri}"
      }
    }
  }
}
